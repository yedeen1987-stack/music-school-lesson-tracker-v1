[Unit]
Description=每天定时把明天的课程提醒放进邮件队列

[Timer]
# 每分钟检查基础设置中的提醒时间（使用服务 TZ），同日只入队一次。
# 脚本判断“已过设置时间且今天未发送”，所以错过某一分钟会在下次触发时补发，
# 不需要秒级精度。
OnCalendar=*-*-* *:*:00
Persistent=true

[Install]
WantedBy=timers.target
